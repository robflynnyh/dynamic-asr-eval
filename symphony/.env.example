# Required by the Symphony workflow config.
LINEAR_API_KEY=

# Repository that Symphony should clone into per-issue workspaces.
SOURCE_REPO_URL=https://github.com/robflynnyh/dynamic-asr-eval.git

# Optional branch, tag, or commit to use as the base for all issue work.
# Leave unset to use the repository default branch.
SOURCE_REF=
